# Project instructions

## Short commands

- `CP` means: commit the changes for the current task, then push the current branch to its configured remote.
- When handling `CP`, stage only files that belong to the current task. Do not include unrelated or pre-existing working-tree changes.
- Run relevant checks before committing and confirm that the local branch matches its upstream after pushing.
